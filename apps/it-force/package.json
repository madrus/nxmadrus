{
  "name": "it-force",
  "version": "0.0.1",
  "description": "it-force",
  "main": "src/main.ts",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "yarn build-css && tsc && vite build",
    "dev": "yarn build-css && vite",
    "coverage": "vitest run --coverage",
    "preview": "vite preview",
    "test": "vitest",
    "test-ui": "vitest --ui",
    "build-css": "postcss src/assets/tailwind.css -o dist/assets/main.css --map",
    "watch-css": "postcss src/assets/tailwind.css -o dist/assets/main.css --map -w"
  }
}
